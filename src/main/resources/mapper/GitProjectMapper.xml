<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feeltens.git.mapper.GitProjectMapper">
    <resultMap id="BaseResultMap" type="com.feeltens.git.entity.GitProjectDO">
        <id column="project_id" jdbcType="BIGINT" property="projectId"/>
        <result column="project_name" jdbcType="VARCHAR" property="projectName"/>
        <result column="repository_url" jdbcType="VARCHAR" property="repositoryUrl"/>
        <result column="organization_id" jdbcType="VARCHAR" property="organizationId"/>
        <result column="repository_id" jdbcType="BIGINT" property="repositoryId"/>
        <result column="project_username" jdbcType="VARCHAR" property="projectUsername"/>
        <result column="project_email" jdbcType="VARCHAR" property="projectEmail"/>
        <result column="default_branch" jdbcType="VARCHAR" property="defaultBranch"/>
        <result column="clone_status" jdbcType="VARCHAR" property="cloneStatus"/>
        <result column="clone_time" jdbcType="TIMESTAMP" property="cloneTime"/>
        <result column="clone_error_msg" jdbcType="VARCHAR" property="cloneErrorMsg"/>
        <result column="last_pull_time" jdbcType="TIMESTAMP" property="lastPullTime"/>
        <result column="web_url" jdbcType="VARCHAR" property="webUrl"/>
        <result column="create_by" jdbcType="VARCHAR" property="createBy"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_by" jdbcType="VARCHAR" property="updateBy"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
        <result column="deleted" jdbcType="BIGINT" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        project_id,
        project_name,
        repository_url,
        organization_id,
        repository_id,
        project_username,
        project_email,
        default_branch,
        clone_status,
        clone_time,
        clone_error_msg,
        last_pull_time,
        web_url,
        create_by,
        create_time,
        update_by,
        update_time,
        deleted
    </sql>

    <insert id="insert" keyColumn="project_id" keyProperty="projectId" useGeneratedKeys="true">
        insert into git_project (project_name, repository_url, organization_id, repository_id, project_username,
                                 project_email, default_branch, clone_status, clone_time, clone_error_msg, last_pull_time,
                                 web_url, create_by, create_time, update_by, update_time, deleted)
        values (#{projectName}, #{repositoryUrl}, #{organizationId}, #{repositoryId}, #{projectUsername},
                #{projectEmail}, #{defaultBranch}, #{cloneStatus}, #{cloneTime}, #{cloneErrorMsg}, #{lastPullTime},
                #{webUrl}, #{createBy}, #{createTime}, #{updateBy}, #{updateTime}, #{deleted})
    </insert>

    <select id="selectByProjectId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from git_project
        where project_id = #{gitProjectId}
        and deleted = 0
    </select>


    <select id="countProject" resultType="java.lang.Long">
        select count(1)
        from git_project
        <where>
            <include refid="condition">
            </include>
        </where>
    </select>

    <sql id="condition">
        <if test="req.gitProjectName != null and req.gitProjectName != ''">
            and project_name like concat('%', #{req.gitProjectName}, '%')
        </if>
        and deleted = 0
    </sql>

    <select id="pageProject" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"></include>
        from git_project
        <where>
            <include refid="condition">
            </include>
            order by project_id desc
            <if test="limitSize != null and pageSize != null">
                limit #{limitSize}, #{pageSize}
            </if>
        </where>
    </select>

    <select id="queryByProjectName" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from git_project
        where project_name = #{projectName}
        and deleted = 0
        limit 1
    </select>

    <select id="queryByProjectId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from git_project
        where project_id = #{projectId}
        and deleted = 0
        limit 1
    </select>

    <update id="deleteByProjectId">
        update git_project
        set deleted = project_id,
        update_time = now()
        where project_id = #{projectId}
    </update>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM git_project
        WHERE deleted = 0
        ORDER BY project_id
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT p.project_id, p.project_name, p.repository_url, p.organization_id, p.repository_id,
               p.project_username, p.project_email, p.default_branch, p.clone_status, p.clone_time,
               p.clone_error_msg, p.last_pull_time, p.web_url,
               p.create_by, p.create_time, p.update_by, p.update_time, p.deleted
        FROM git_project p
        INNER JOIN sys_user_project_perm perm ON p.project_id = perm.project_id
        WHERE perm.user_id = #{userId} AND p.deleted = 0
        ORDER BY p.project_id
    </select>

    <select id="countProjectByUserId" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM git_project p
        INNER JOIN sys_user_project_perm perm ON p.project_id = perm.project_id
        WHERE perm.user_id = #{userId} AND p.deleted = 0
        <if test="req.gitProjectName != null and req.gitProjectName != ''">
            AND p.project_name LIKE CONCAT('%', #{req.gitProjectName}, '%')
        </if>
    </select>

    <select id="pageProjectByUserId" resultMap="BaseResultMap">
        SELECT p.project_id, p.project_name, p.repository_url, p.organization_id, p.repository_id,
               p.project_username, p.project_email, p.default_branch, p.clone_status, p.clone_time,
               p.clone_error_msg, p.last_pull_time, p.web_url,
               p.create_by, p.create_time, p.update_by, p.update_time, p.deleted
        FROM git_project p
        INNER JOIN sys_user_project_perm perm ON p.project_id = perm.project_id
        WHERE perm.user_id = #{userId} AND p.deleted = 0
        <if test="req.gitProjectName != null and req.gitProjectName != ''">
            AND p.project_name LIKE CONCAT('%', #{req.gitProjectName}, '%')
        </if>
        ORDER BY p.project_id DESC
        <if test="limitSize != null and pageSize != null">
            LIMIT #{limitSize}, #{pageSize}
        </if>
    </select>

    <update id="updateCloneStatus">
        UPDATE git_project
        SET clone_status = #{project.cloneStatus},
            clone_time = #{project.cloneTime},
            clone_error_msg = #{project.cloneErrorMsg},
            last_pull_time = #{project.lastPullTime},
            update_time = NOW()
        WHERE project_id = #{project.projectId}
    </update>

</mapper>