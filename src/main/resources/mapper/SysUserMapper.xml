<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feeltens.git.mapper.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.feeltens.git.entity.SysUserDO">
        <id column="user_id" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="status" property="status"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_by" property="updateBy"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        user_id, username, password, nickname, email, status,
        create_by, create_time, update_by, update_time, deleted
    </sql>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="selectByNickname" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE nickname = #{nickname} AND deleted = 0
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE user_id = #{userId} AND deleted = 0
    </select>

    <select id="selectPageList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM sys_user
        WHERE deleted = 0
        <if test="param != null">
            <if test="param.username != null and param.username != ''">
                AND username LIKE CONCAT('%', #{param.username}, '%')
            </if>
            <if test="param.nickname != null and param.nickname != ''">
                AND nickname LIKE CONCAT('%', #{param.nickname}, '%')
            </if>
            <if test="param.status != null">
                AND status = #{param.status}
            </if>
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectCount" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM sys_user
        WHERE deleted = 0
        <if test="param != null">
            <if test="param.username != null and param.username != ''">
                AND username LIKE CONCAT('%', #{param.username}, '%')
            </if>
            <if test="param.nickname != null and param.nickname != ''">
                AND nickname LIKE CONCAT('%', #{param.nickname}, '%')
            </if>
            <if test="param.status != null">
                AND status = #{param.status}
            </if>
        </if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO sys_user (username, password, nickname, email, status, create_by, update_by)
        VALUES (#{username}, #{password}, #{nickname}, #{email}, #{status}, #{createBy}, #{updateBy})
    </insert>

    <update id="updateById">
        UPDATE sys_user
        <set>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="email != null">email = #{email},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
        </set>
        WHERE user_id = #{userId} AND deleted = 0
    </update>

    <update id="updatePassword">
        UPDATE sys_user
        SET password = #{password}, update_by = #{updateBy}
        WHERE user_id = #{userId} AND deleted = 0
    </update>

    <update id="deleteById">
        UPDATE sys_user
        SET deleted = user_id, update_by = #{updateBy}
        WHERE user_id = #{userId} AND deleted = 0
    </update>

</mapper>