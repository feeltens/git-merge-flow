<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.feeltens.git.mapper.SysUserProjectPermMapper">

    <resultMap id="BaseResultMap" type="com.feeltens.git.entity.SysUserProjectPermDO">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="project_id" property="projectId"/>
        <result column="perm_type" property="permType"/>
        <result column="create_by" property="createBy"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT id, user_id, project_id, perm_type, create_by, create_time
        FROM sys_user_project_perm
        WHERE user_id = #{userId}
    </select>

    <select id="selectProjectIdsByUserId" resultType="java.lang.Long">
        SELECT project_id
        FROM sys_user_project_perm
        WHERE user_id = #{userId}
    </select>

    <insert id="batchInsert">
        INSERT INTO sys_user_project_perm (user_id, project_id, perm_type, create_by)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.projectId}, #{item.permType}, #{item.createBy})
        </foreach>
    </insert>

    <delete id="deleteByUserId">
        DELETE FROM sys_user_project_perm WHERE user_id = #{userId}
    </delete>

    <delete id="deleteByProjectId">
        DELETE FROM sys_user_project_perm WHERE project_id = #{projectId}
    </delete>

    <select id="existsByUserIdAndProjectId" resultType="boolean">
        SELECT COUNT(1) > 0 FROM sys_user_project_perm
        WHERE user_id = #{userId} AND project_id = #{projectId}
    </select>

    <insert id="insert">
        INSERT INTO sys_user_project_perm (user_id, project_id, perm_type, create_by, create_time)
        VALUES (#{userId}, #{projectId}, #{permType}, #{createBy}, #{createTime})
    </insert>

</mapper>